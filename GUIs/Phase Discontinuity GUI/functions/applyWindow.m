% This function applies a window type to the signal segment
function segment = applyWindow(segment, window)
    
    if(window == window_type.Rect)

    elseif(window == window_type.HalfSine)

    elseif(window == window_type.Hamming)

    elseif(window == window_type.Kaiser)

    elseif(window == window_type.Blackman)

    end
